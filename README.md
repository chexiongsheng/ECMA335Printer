# ECMA335Printer

A .NET assembly metadata printer that parses and displays PE file structure and CLI metadata information without using `System.Reflection.Metadata` or `System.Reflection.PortableExecutable`.
